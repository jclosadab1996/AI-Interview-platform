import React from "react";

const InterviewCard = ({
  interviewId,
  userId,
  role,
  type,
  techstack,
  createdAt,
}: interviewCardProps) => {
  return <div>InterviewCard</div>;
};

export default InterviewCard;
